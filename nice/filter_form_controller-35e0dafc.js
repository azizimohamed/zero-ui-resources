import { Controller } from "@hotwired/stimulus";

export default class extends Controller {
  addRow(_event) {
    /* Reserved for dynamic filter rows — single-screen Pass 1 keeps static fields */
  }
}
